# SpaceApp-Eminence


To run this project

Setup a venv
- `pip install poetry`
- `poetry install`
> Or just install django `pip install django`

Run file
- `cd TrueNorth`
- `python manage.py runserver`
